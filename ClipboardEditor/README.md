# ClipboardEditor

This component library wraps Monaco Editor for Blazor apps.

Setup:
- Run `npm install` in this folder.
- Run `npm run build:monaco` to copy Monaco assets into `wwwroot/monaco`.

The app will serve Monaco assets from `_content/ClipboardEditor/monaco`.
